// Entry point. The bootstrap is the whole app: it imports core (analyze / render_body) and
// the chart renderer directly and, on DOMContentLoaded, resolves the data source and renders.
import "./app/bootstrap";
